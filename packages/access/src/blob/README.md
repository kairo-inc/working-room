# Blob Hash

When a tool reads or writes a file, the tool returns the blob hash of the file as part of its response.

In subsequent chats, the file content is restored from that hash so the conversation can continue with the correct file state.
